#Code 

class Solution:
    def search(self, nums: List[int], target: int) -> int:
        if target in nums:
            stringConcat=nums[:target]+nums[target:]
            return nums.index(target)
        else:
            return -1
